# Database Schemas

Run the SQL in `schemas.sql` in your Supabase SQL editor.

The schema creates:

- `public.users` for registration/login accounts
- `public.items` for the CRUD records
- a trigger to update `updated_at` automatically
